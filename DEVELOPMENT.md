# ClaudeCode Config Manager - 开发指南

**项目**: ClaudeCode 配置可视化管理工具
**迭代**: Phase 1 - 基础框架
**版本**: 0.1.0
**日期**: 2026-02-06

---

## 📦 项目结构

```
claude-config-manager/
├── src/                          # React 前端源码
│   ├── components/               # UI 组件
│   │   ├── layout/              # 布局组件
│   │   │   ├── Header.tsx       # 顶部导航栏
│   │   │   └── Sidebar.tsx      # 侧边栏导航
│   │   ├── ui/                  # 基础 UI 组件
│   │   │   ├── Button.tsx       # 按钮
│   │   │   ├── Card.tsx         # 卡片
│   │   │   ├── Badge.tsx        # 标签
│   │   │   └── Loading.tsx      # 加载指示器
│   │   └── Dashboard.tsx        # 概览页面
│   ├── background/               # Node.js 后端 (Neutralino)
│   │   └── index.ts             # 后端 API 实现
│   ├── lib/                      # 工具函数
│   │   ├── api.ts               # 前端 API 封装
│   │   └── utils.ts             # 通用工具
│   ├── store/                    # 状态管理
│   │   └── appStore.ts          # Zustand 全局状态
│   ├── types/                    # TypeScript 类型
│   │   └── index.ts             # 核心类型定义
│   ├── App.tsx                   # 根组件
│   ├── main.tsx                  # 应用入口
│   └── index.css                 # 全局样式
├── resources/                   # Neutralino 资源
│   └── js/                      # 后端 JS (编译后)
├── dist/                        # 构建输出
├── bin/                         # 可执行文件 (构建后)
├── neutralino.config.json       # Neutralino 配置
├── package.json                 # Node.js 依赖
├── vite.config.ts              # Vite 配置
├── tsconfig.json               # TypeScript 配置
├── tailwind.config.js          # Tailwind 配置
└── CLAUDE.md                   # Claude Code 开发指南
```

---

## 🚀 快速开始

### 环境要求

```bash
# 必需工具
- Node.js 18+
- npm 或 yarn

# 可选工具（用于构建桌面应用）
- @neutralinojs/neu CLI
```

### 安装依赖

```bash
# 安装 Node.js 依赖
npm install

# 或使用 yarn
yarn install
```

### 开发模式

```bash
# 启动 Vite 开发服务器（浏览器模式）
npm run dev

# 应用将在 http://localhost:3737 启动
```

### 构建

```bash
# 构建前端资源
npm run build

# 输出目录: dist/
```

---

## 🔧 开发命令

```bash
# 开发
npm run dev              # 启动 Vite 开发服务器

# 构建
npm run build            # 构建生产版本
npm run preview          # 预览构建结果

# 代码质量
npm run lint             # ESLint 检查
npm run format           # Prettier 格式化

# Neutralino (需要安装 neu CLI)
npm install -g @neutralinojs/neu   # 全局安装 Neutralino CLI
neu run                               # 运行桌面应用
neu build                             # 构建桌面应用
neu release                           # 生成发布包
```

---

## 📚 核心技术

### 前端技术栈

- **React 18**: UI 框架
- **TypeScript**: 类型安全
- **Vite**: 快速构建工具
- **Tailwind CSS**: 实用优先的 CSS 框架
- **Zustand**: 轻量级状态管理

### 后端技术栈

- **Node.js**: 运行时
- **Neutralino**: 轻量级桌面框架
- **TypeScript**: 类型安全

### UI 组件

本项目使用自定义组件，基于 shadcn/ui 设计系统：
- Button, Card, Badge, Loading
- 所有组件支持深色模式
- 使用 Tailwind CSS 实用类

---

## 🏗️ 架构说明

### 数据流

```
用户操作 → React 组件 → Zustand Store → API 调用
                                        ↓
                                Neutralino IPC
                                        ↓
                            Node.js Backend (background/)
                                        ↓
                                文件系统操作
                                        ↓
                            返回数据 → Store 更新 → UI 重渲染
```

### 状态管理

使用 Zustand 管理全局状态：

```typescript
// 使用状态
const { data, isLoading, refreshData } = useAppStore();

// 更新状态
const { setSelectedTab } = useAppStore();
setSelectedTab('skills');
```

### API 调用

前端通过封装的 API 模块调用后端：

```typescript
import { api } from './lib/api';

// 加载数据
const data = await api.loadAllData();

// 读取配置
const config = await api.readConfig('~/.mcp.json');

// 写入配置
await api.writeConfig('~/.mcp.json', content, true);
```

---

## 🎨 设计系统

### 颜色规范

```css
/* 主色调 - 温暖灰色 */
gray-50:   #FAFAF9  /* 页面背景 */
gray-100:  #F5F5F4  /* 次级背景 */
gray-200:  #E7E5E4  /* 边框 */
gray-600:  #57534E  /* 正文 */
gray-900:  #1C1917  /* 标题 */

/* 强调色 - 琥珀色 */
amber-600: #D97706  /* 主按钮 */
```

### 组件样式

所有组件使用 Tailwind CSS 类，不使用 CSS 变量：

```tsx
// ✅ 正确
<Button className="bg-amber-600 hover:bg-amber-700 text-white">

// ❌ 错误
<Button className="bg-[var(--primary)]">
```

### 图标规范

使用 Lucide React 图标库，不使用表情符号：

```tsx
// ✅ 正确
import { Settings, Refresh } from 'lucide-react';
<Settings className="w-5 h-5" />

// ❌ 错误
<span>⚙️</span>
```

---

## 🧪 测试

### 运行测试

当前版本未包含自动化测试，请手动测试以下功能：

1. **数据加载**
   - 启动应用，检查 Dashboard 是否显示统计
   - 点击"刷新"按钮，检查数据是否重新加载

2. **导航切换**
   - 点击侧边栏不同选项，检查内容是否切换

3. **响应式**
   - 调整窗口大小，检查布局是否适应

---

## 📝 开发注意事项

### 代码规范

1. **使用 TypeScript**: 所有文件必须使用 `.ts` 或 `.tsx` 扩展名
2. **组件命名**: 使用 PascalCase (如 `Dashboard.tsx`)
3. **函数命名**: 使用 camelCase (如 `loadData`)
4. **常量命名**: 使用 UPPER_SNAKE_CASE 或 camelCase

### 提交规范

提交前请运行：

```bash
# 代码检查
npm run lint

# 代码格式化
npm run format

# 构建测试
npm run build
```

### 性能优化

1. **避免不必要的重渲染**: 使用 `React.memo` 包装组件
2. **懒加载**: 大型组件使用 `React.lazy()`
3. **代码分割**: 路由级别的代码分割

---

## 🐛 调试

### 前端调试

1. 打开浏览器开发者工具 (F12)
2. 查看 Console 日志
3. 使用 React DevTools 查看组件状态

### 后端调试

```typescript
// 在 background/index.ts 中添加日志
console.log('Debug info:', data);
```

日志会输出到终端（运行 `neu run` 的终端）

---

## 🚧 已知限制

### 当前未实现功能

以下功能将在 Phase 2 实现：

- [ ] Skills 管理页面
- [ ] MCP 服务器管理页面
- [ ] 项目管理页面
- [ ] 配置文件编辑器
- [ ] 配置修改功能
- [ ] Diff 预览
- [ ] 备份恢复

### 技术限制

1. **文件监听**: 当前版本不支持实时文件监听，需要手动刷新
2. **跨平台**: 部分功能需要针对不同操作系统调整
3. **大数据**: 处理大量配置文件时可能存在性能问题

---

## 📖 相关文档

- [CLAUDE.md](./CLAUDE.md) - Claude Code 开发指南
- [README.md](./README.md) - 项目说明
- [技术方案对比.md](./技术方案对比.md) - 技术选型说明

---

## 🤝 贡献指南

### 开发流程

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码审查

所有 PR 需要通过：
- [ ] TypeScript 类型检查
- [ ] ESLint 代码检查
- [ ] 功能测试
- [ ] 性能测试

---

## 📄 许可证

TBD

---

**更新日期**: 2026-02-06
**文档版本**: 0.1.0
