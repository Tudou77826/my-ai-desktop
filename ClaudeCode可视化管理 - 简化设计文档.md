# ClaudeCode可视化管理工具 - 简化设计文档 v2.0

## 一、产品定位重新审视

### 核心价值
一个**轻量级、专注**的ClaudeCode配置可视化工具，解决"配置文件分散难管理"这一单一痛点。

### 产品原则
1. **简单优先** - 只做必要的功能，快速上手
2. **被动响应** - 按需读取数据，不持续监听
3. **谨慎操作** - 修改前预览，确认后生效
4. **轻量快速** - 启动<3秒，响应<500ms

### 明确不做（划清边界）
- ❌ 不做实时文件监听（用手动刷新）
- ❌ 不做配置版本管理（Git够用）
- ❌ 不做AI助手/智能推荐（过度设计）
- ❌ 不做云端同步/团队协作（后期考虑）
- ❌ 不做模板市场（后期考虑）

---

## 二、技术栈重新评估

### 方案对比

| 方案 | 优势 | 劣势 | 推荐度 |
|------|------|------|--------|
| **Tauri + React** | 极轻量(10MB)，性能好，启动快，Rust后端安全 | 需要Rust知识，生态较新 | ⭐⭐⭐⭐⭐ |
| **Electron + React** | 成熟，开发体验好，库丰富 | 体积大(150MB+)，资源占用高 | ⭐⭐⭐ |
| **VS Code扩展** | 无需额外应用，集成度高 | 受限于VS Code界面，无法独立运行 | ⭐⭐⭐⭐ |
| **Next.js + 本地服务** | Web技术栈，开发快，可远程访问 | 需要后台服务，安全性考虑 | ⭐⭐⭐ |
| **TUI (Blessed)** | 最轻量，符合开发者习惯 | 交互受限，无法富文本编辑 | ⭐⭐ |
| **单页HTML + Node.js** | 最简单，无需打包 | 体验较差，难以维护 | ⭐⭐ |

### 推荐方案：**Tauri + React + TypeScript**

**选择理由：**
1. **体积小** - 打包后仅10-20MB vs Electron的150MB+
2. **启动快** - <1秒启动时间
3. **资源占用低** - 内存占用<100MB
4. **安全性高** - Rust后端，内存安全
5. **Web技术栈** - 前端用React，开发体验好
6. **原生能力** - 可以调用系统命令读写文件

**技术栈组成：**
```
前端: React 18 + TypeScript + Vite
UI库: shadcn/ui (基于Radix UI + Tailwind CSS)
状态管理: Zustand (轻量级，<1KB)
图标: Lucide React
后端: Rust (Tauri)
构建: Tauri CLI
```

**备选方案：VS Code扩展**

如果你的目标是更快的开发和更好的IDE集成，可以考虑VS Code扩展：
```typescript
// 技术栈
- VS Code Extension API
- Webview API (React UI)
- Node.js后端
- 体积: 几百KB
```

---

## 三、极简架构设计

### 整体架构

```
┌─────────────────────────────────────────────────┐
│              React UI (前端)                     │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │ Dashboard│  Skills  │   MCP    │ Projects │  │
│  └──────────┴──────────┴──────────┴──────────┘  │
└─────────────────┬───────────────────────────────┘
                  │ Tauri IPC (进程间通信)
┌─────────────────▼───────────────────────────────┐
│           Rust Backend (Tauri Commands)          │
│  ┌─────────────────┬─────────────────────────┐  │
│  │  命令执行模块    │      文件操作模块        │  │
│  │  - execCLI()    │  - readConfig()         │  │
│  │  - parseJSON()  │  - writeConfig()        │  │
│  └─────────────────┴─────────────────────────┘  │
└─────────────────┬───────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────┐
│         ClaudeCode CLI + 配置文件                │
│  ~/.claude/, ~/.mcp.json, project/.claude/     │
└─────────────────────────────────────────────────┘
```

### 数据流（单向）

```
用户操作 → UI事件 → Tauri Command → CLI/文件 → 返回数据 → UI更新
   ↓
手动刷新或编辑操作
```

**关键设计：**
- ✅ 被动读取：用户点"刷新"才调用CLI获取数据
- ✅ 内存缓存：数据存在Zustand store中，刷新时更新
- ✅ 无持久化：不缓存到数据库，每次都是最新数据
- ✅ 简单验证：编辑前用JSON Schema验证

---

## 四、核心数据模型（最小化）

```typescript
// ==================== 核心类型定义 ====================

// 技能
interface Skill {
  id: string;                  // 技能目录名
  path: string;                // 完整路径
  scope: 'global' | 'project'; // 作用域
  enabled: boolean;            // 是否启用
  metadata?: {
    name?: string;
    description?: string;
    author?: string;
  };
  content?: string;            // SKILL.md内容（按需加载）
}

// MCP服务器
interface MCPServer {
  id: string;                  // 服务器名称
  scope: 'global' | 'project';
  transport: 'stdio' | 'sse' | 'http';
  config: {
    command?: string;
    args?: string[];
    url?: string;
    env?: Record<string, string>;
  };
  enabled: boolean;
  health?: {
    status: 'unknown' | 'ok' | 'error';
    lastCheck?: Date;
    error?: string;
  };
}

// 项目
interface Project {
  id: string;                  // 项目路径作为ID
  name: string;                // 项目名（目录名）
  path: string;                // 完整路径
  exists: boolean;             // 路径是否存在
  config?: {
    skills?: string[];         // 启用的技能ID列表
    mcpServers?: string[];     // 启用的MCP服务器ID
    settings?: Record<string, any>; // 项目设置
  };
  claudeMd?: {
    path: string;
    content: string;
  };
  lastModified?: Date;
}

// 配置文件
interface ConfigFile {
  path: string;                // 文件路径
  type: 'settings' | 'mcp' | 'claude_md';
  scope: 'global' | 'project';
  format: 'json' | 'markdown';
  content?: any;               // 解析后的内容
  raw?: string;                // 原始内容
}

// 应用状态
interface AppState {
  data: {
    skills: Skill[];
    mcpServers: MCPServer[];
    projects: Project[];
    configFiles: ConfigFile[];
  };
  ui: {
    selectedTab: string;
    isLoading: boolean;
    lastRefresh: Date;
  };
}
```

---

## 五、核心API设计（Tauri Commands）

### Rust后端提供10个核心命令

```rust
// ==================== Tauri Commands ====================

// 1. 读取所有数据（一次性获取）
#[tauri::command]
async fn load_all_data() -> Result<AppData, String>

// 2. 刷新数据（重新调用CLI）
#[tauri::command]
async fn refresh_data() -> Result<AppData, String>

// 3. 读取单个配置文件
#[tauri::command]
async fn read_config(path: String) -> Result<ConfigFile, String>

// 4. 写入配置文件（带备份）
#[tauri::command]
async fn write_config(path: String, content: String, backup: bool)
    -> Result<(), String>

// 5. 验证配置有效性
#[tauri::command]
async fn validate_config(config_type: String, content: String)
    -> Result<ValidationResult, String>

// 6. 预览配置变更（diff）
#[tauri::command]
async fn preview_changes(path: String, new_content: String)
    -> Result<String, String> // 返回diff文本

// 7. 启用/禁用技能
#[tauri::command]
async fn toggle_skill(skill_id: String, enabled: bool)
    -> Result<(), String>

// 8. 启用/禁用MCP服务器
#[tauri::command]
async fn toggle_mcp_server(server_id: String, enabled: bool)
    -> Result<(), String>

// 9. 测试MCP服务器连接
#[tauri::command]
async fn test_mcp_connection(server: MCPServer)
    -> Result<HealthStatus, String>

// 10. 执行ClaudeCode命令
#[tauri::command]
async fn exec_claude_command(args: Vec<String>)
    -> Result<String, String>
```

### 前端调用示例

```typescript
// React组件中调用
import { invoke } from '@tauri-apps/api/tauri';

// 加载数据
const data = await invoke<AppData>('load_all_data');

// 刷新
const freshData = await invoke<AppData>('refresh_data');

// 写入配置（带预览）
const diff = await invoke<string>('preview_changes', {
  path: '~/.mcp.json',
  newContent: JSON.stringify(newConfig, null, 2)
});

// 显示diff确认
if (await confirm(`确认修改？\n\n${diff}`)) {
  await invoke('write_config', {
    path: '~/.mcp.json',
    content: JSON.stringify(newConfig, null, 2),
    backup: true
  });
}
```

---

## 六、UI设计（极简）

### 整体布局

```
┌─────────────────────────────────────────────────────┐
│  🔍 ClaudeCode Config Manager      [🔄 刷新] [⚙️]   │
├─────────────────────────────────────────────────────┤
│  [📊 概览] [🧩 Skills] [🔌 MCP] [📁 项目] [📝 配置]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│                    主内容区                          │
│                （根据Tab切换）                        │
│                                                     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 页面1：概览 Dashboard

```typescript
┌─────────────────────────────────────────────────────┐
│  📊 配置概览                              [🔄 刷新]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  📈 统计卡片                                        │
│  ┌─────────┬─────────┬─────────┬─────────────┐     │
│  │  Skills │   MCP   │ 项目数  │ 配置文件     │     │
│  │    12   │    5    │    8    │    24       │     │
│  └─────────┴─────────┴─────────┴─────────────┘     │
│                                                     │
│  🟢 配置健康状态                                    │
│  ✅ 所有配置文件有效                                │
│  ⚠️  2个MCP服务器未测试连接                         │
│                                                     │
│  📁 最近使用的项目                                  │
│  • my-react-app (5分钟前)                           │
│  • node-api-server (2小时前)                        │
│  • python-ml-project (昨天)                         │
│                                                     │
│  🔧 快速操作                                        │
│  [打开全局配置] [备份所有配置] [验证配置]            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 页面2：Skills管理

```typescript
┌─────────────────────────────────────────────────────┐
│  🧩 Skills 管理                    [➕ 安装] [🔄]   │
├─────────────────────────────────────────────────────┤
│  [全部] [已启用] [全局] [项目] [🔍 搜索]            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  📁 全局 Skills                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ ✅ commit                                      │ │
│  │    Git提交管理                                │ │
│  │    路径: ~/.claude/skills/commit              │ │
│  │    [查看详情] [禁用] [编辑 SKILL.md]          │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ✅ pr-review                                   │ │
│  │    PR代码审查                                  │ │
│  │    [查看详情] [禁用] [编辑]                    │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ⬪ test-runner                                 │ │
│  │    测试运行专家                                │ │
│  │    [查看详情] [启用] [编辑]                    │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  📁 项目 Skills                                    │
│  • react-component (仅my-react-app)                 │
│  • next-ssr-helper (仅my-next-app)                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**点击"查看详情"展开：**
```typescript
┌─────────────────────────────────────────────────────┐
│  Skill: commit                      [✕ 关闭]        │
├─────────────────────────────────────────────────────┤
│  📄 SKILL.md 内容预览                               │
│  ┌───────────────────────────────────────────────┐ │
│  │ # Commit Skill                                │ │
│  │                                               │ │
│  │ 用于创建Git提交的技能...                       │ │
│  │                                               │ │
│  │ ## Usage                                      │ │
│  │ /commit                                       │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  📂 支持的文件                                      │
│  • SKILL.md                                         │
│  • commit_template.txt                              │
│                                                     │
│  📊 使用统计                                        │
│  • 使用次数: 127                                    │
│  • 最后使用: 2小时前                                │
│                                                     │
│  🔧 操作                                            │
│  [在VS Code中打开] [禁用] [删除]                    │
└─────────────────────────────────────────────────────┘
```

### 页面3：MCP服务器

```typescript
┌─────────────────────────────────────────────────────┐
│  🔌 MCP 服务器                      [➕ 添加] [🔄]  │
├─────────────────────────────────────────────────────┤
│  [全部] [已启用] [全局] [项目] [🔍 搜索]            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🌐 全局 MCP 服务器                                 │
│  ┌───────────────────────────────────────────────┐ │
│  │ ✅ GitHub                                      │ │
│  │    🟢 在线 (45ms)                              │ │
│  │    传输: HTTP | 作用域: 全局                   │ │
│  │    [测试连接] [查看配置] [禁用] [编辑]         │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ✅ Filesystem                                  │ │
│  │    🟡 未测试                                   │ │
│  │    传输: stdio | 作用域: 全局                  │ │
│  │    [测试连接] [查看配置] [禁用] [编辑]         │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │ ⬪ Google Drive                                │ │
│  │    🔴 离线                                     │ │
│  │    传输: SSE | 作用域: 全局                    │ │
│  │    [测试连接] [查看配置] [启用] [编辑]         │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  📁 项目 MCP 服务器                                 │
│  • Postgres (仅在node-api项目)                      │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**编辑MCP配置（JSON编辑器）：**
```typescript
┌─────────────────────────────────────────────────────┐
│  编辑 MCP: GitHub                      [💾 保存]    │
├─────────────────────────────────────────────────────┤
│  {                                                 │
│    "transport": "http",                            │
│    "url": "https://api.githubcopilot.com/mcp",     │
│    "enabled": true                                 │
│  }                                                 │
│                                                     │
│  ✅ JSON 格式有效                                   │
│  ⚠️  警告: URL可能需要认证                          │
│                                                     │
│  [🔄 撤销] [💾 保存] [💾 保存并关闭]                │
└─────────────────────────────────────────────────────┘
```

### 页面4：项目管理

```typescript
┌─────────────────────────────────────────────────────┐
│  📁 项目管理                         [➕ 添加] [🔄]  │
├─────────────────────────────────────────────────────┤
│  [最近使用] [按名称] [按类型]          [🔍 搜索]   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ 📦 my-react-app                                │ │
│  │    React | 路径: /Users/dev/my-react-app       │ │
│  │    最后访问: 5分钟前                           │ │
│  │                                                │ │
│  │    🧩 Skills: commit, test-runner,             │ │
│  │               react-component (项目)           │ │
│  │    🔌 MCP: GitHub, Figma (项目)                │ │
│  │                                                │ │
│  │    [查看配置] [打开CLAUDE.md] [在VS Code打开]   │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ 📦 node-api-server                             │ │
│  │    Node.js | 最后访问: 2小时前                 │ │
│  │    🧩 Skills: commit, pr-review                │ │
│  │    🔌 MCP: GitHub, Postgres (项目)             │ │
│  │    [查看配置] [打开CLAUDE.md]                   │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 页面5：配置文件编辑器

```typescript
┌─────────────────────────────────────────────────────┐
│  📝 配置文件编辑器                                   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  文件树:                                            │
│  📁 ~/.claude/                                      │
│    ├─ 📄 settings.json               [已修改]       │
│    ├─ 📄 mcp.json                                   │
│    └─ 📁 skills/                                    │
│  📁 /Users/dev/my-react-app/                        │
│    ├─ 📄 CLAUDE.md                                  │
│    └─ 📁 .claude/                                   │
│        └─ 📄 settings.json                          │
│                                                     │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  编辑区: ~/.claude/settings.json                    │
│  ┌───────────────────────────────────────────────┐ │
│  │ {                                             │ │
│  │   "selectedModel": "claude-sonnet-4-5",       │ │
│  │   "useTerminal": true,                        │ │
│  │   "initialPermissionMode": "default"          │ │
│  │ }                                             │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
│  ✅ JSON 有效   [📋 复制] [💾 保存] [↩️ 恢复]       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 七、核心工作流程

### 1. 数据加载流程

```typescript
// 启动时或用户点击"刷新"
用户点击刷新
    ↓
invoke('refresh_data')
    ↓
Rust后端并行执行：
  ├─ 读取 ~/.claude/settings.json
  ├─ 读取 ~/.mcp.json
  ├─ 扫描 ~/.claude/skills/*
  ├─ 搜索项目中 .claude/ 目录
  └─ 读取项目的 CLAUDE.md
    ↓
返回 AppData
    ↓
更新 Zustand Store
    ↓
UI自动刷新
```

### 2. 编辑配置流程

```typescript
用户编辑配置文件
    ↓
点击"保存"
    ↓
invoke('validate_config', {type, content})
    ↓
如果验证失败 → 显示错误，阻止保存
    ↓
验证通过
    ↓
invoke('preview_changes', {path, newContent})
    ↓
显示 Diff 预览对话框
    ↓
用户确认？
  ├─ 否 → 取消操作
  └─ 是 → 继续
      ↓
invoke('write_config', {path, content, backup: true})
    ↓
自动创建 .backup 文件
    ↓
写入新内容
    ↓
成功提示 + 刷新数据
```

### 3. 启用/禁用Skill/MCP流程

```typescript
用户切换开关
    ↓
invoke('toggle_skill', {skillId, enabled})
    ↓
Rust后端：
  ├─ 读取配置文件
  ├─ 修改对应项
  ├─ 验证JSON有效性
  └─ 写入文件
    ↓
返回成功/失败
    ↓
显示Toast通知
    ↓
刷新本地状态
```

---

## 八、技术实现细节

### Tauri项目结构

```
claude-config-manager/
├── src/                    # React前端
│   ├── components/         # UI组件
│   │   ├── Dashboard.tsx
│   │   ├── SkillsList.tsx
│   │   ├── MCPServers.tsx
│   │   ├── Projects.tsx
│   │   └── ConfigEditor.tsx
│   ├── lib/                # 工具函数
│   │   └── tauri.ts        # Tauri API封装
│   ├── store/              # Zustand状态
│   │   └── appStore.ts
│   ├── types/              # TypeScript类型
│   │   └── index.ts
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/              # Rust后端
│   ├── src/
│   │   ├── main.rs         # Tauri主程序
│   │   ├── commands/       # Tauri命令
│   │   │   ├── mod.rs
│   │   │   ├── config.rs   # 配置读写
│   │   │   ├── cli.rs      # CLI调用
│   │   │   └── validate.rs # 验证逻辑
│   │   └── utils/          # 工具函数
│   ├── Cargo.toml
│   └── tauri.conf.json
├── package.json
└── tsconfig.json
```

### 核心Rust代码示例

```rust
// src-tauri/src/commands/config.rs

use serde_json::Value;
use std::fs;
use std::path::Path;

#[tauri::command]
async fn read_config(path: String) -> Result<ConfigFile, String> {
    let path_obj = Path::new(&path);

    // 检查文件是否存在
    if !path_obj.exists() {
        return Err("配置文件不存在".to_string());
    }

    // 读取文件内容
    let content = fs::read_to_string(path_obj)
        .map_err(|e| format!("读取失败: {}", e))?;

    // 解析JSON
    let parsed: Value = serde_json::from_str(&content)
        .map_err(|e| format!("JSON解析失败: {}", e))?;

    Ok(ConfigFile {
        path,
        content: parsed,
        raw: content,
    })
}

#[tauri::command]
async fn write_config(
    path: String,
    content: String,
    backup: bool
) -> Result<(), String> {
    let path_obj = Path::new(&path);

    // 创建备份
    if backup && path_obj.exists() {
        let backup_path = format!("{}.backup", path);
        fs::copy(&path, &backup_path)
            .map_err(|e| format!("备份失败: {}", e))?;
    }

    // 验证JSON
    if path.ends_with(".json") {
        serde_json::from_str::<Value>(&content)
            .map_err(|e| format!("JSON无效: {}", e))?;
    }

    // 写入文件
    fs::write(path_obj, content)
        .map_err(|e| format!("写入失败: {}", e))?;

    Ok(())
}

#[tauri::command]
async fn preview_changes(
    path: String,
    new_content: String
) -> Result<String, String> {
    // 读取原内容
    let old_content = fs::read_to_string(&path)
        .map_err(|e| format!("读取失败: {}", e))?;

    // 生成简单diff（逐行对比）
    let diff = generate_diff(&old_content, &new_content);

    Ok(diff)
}

fn generate_diff(old: &str, new: &str) -> String {
    // 简单实现：逐行对比
    let old_lines: Vec<&str> = old.lines().collect();
    let new_lines: Vec<&str> = new.lines().collect();

    let mut diff = String::new();
    let max_len = old_lines.len().max(new_lines.len());

    for i in 0..max_len {
        let old_line = old_lines.get(i).unwrap_or(&"");
        let new_line = new_lines.get(i).unwrap_or(&"");

        if old_line != new_line {
            diff.push_str(&format!("- {}\n", old_line));
            diff.push_str(&format!("+ {}\n", new_line));
        }
    }

    diff
}
```

### React前端核心代码

```typescript
// src/store/appStore.ts
import { create } from 'zustand';

interface AppStore {
  data: AppData | null;
  isLoading: boolean;
  lastRefresh: Date | null;
  error: string | null;

  // Actions
  loadData: () => Promise<void>;
  refreshData: () => Promise<void>;
  setError: (error: string | null) => void;
}

export const useAppStore = create<AppStore>((set) => ({
  data: null,
  isLoading: false,
  lastRefresh: null,
  error: null,

  loadData: async () => {
    set({ isLoading: true, error: null });
    try {
      const data = await invoke<AppData>('load_all_data');
      set({ data, isLoading: false, lastRefresh: new Date() });
    } catch (error) {
      set({ error: String(error), isLoading: false });
    }
  },

  refreshData: async () => {
    set({ isLoading: true, error: null });
    try {
      const data = await invoke<AppData>('refresh_data');
      set({ data, isLoading: false, lastRefresh: new Date() });
    } catch (error) {
      set({ error: String(error), isLoading: false });
    }
  },

  setError: (error) => set({ error }),
}));
```

```typescript
// src/components/Dashboard.tsx
import { useEffect } from 'react';
import { useAppStore } from '../store/appStore';

export function Dashboard() {
  const { data, isLoading, lastRefresh, refreshData } = useAppStore();

  useEffect(() => {
    refreshData();
  }, []);

  if (isLoading) return <div>加载中...</div>;

  return (
    <div className="dashboard">
      <div className="header">
        <h1>配置概览</h1>
        <button onClick={refreshData}>
          🔄 刷新 {lastRefresh && `(${formatTime(lastRefresh)})`}
        </button>
      </div>

      <div className="stats">
        <StatCard label="Skills" value={data?.skills.length || 0} />
        <StatCard label="MCP" value={data?.mcpServers.length || 0} />
        <StatCard label="项目" value={data?.projects.length || 0} />
      </div>

      {/* 其他内容 */}
    </div>
  );
}
```

---

## 九、MVP实施计划（2-3周）

### Week 1: 基础框架 + 数据层

**目标：搭建Tauri项目，实现数据读取**

- [ ] Day 1-2: 项目初始化
  - 创建Tauri + React项目
  - 配置shadcn/ui
  - 设置TypeScript类型

- [ ] Day 3-4: Rust后端 - 数据读取
  - 实现 `read_config` 命令
  - 实现 `load_all_data` 命令
  - 解析配置文件逻辑

- [ ] Day 5-7: 前端 - 数据展示
  - 创建Zustand store
  - 实现Dashboard页面
  - 显示配置统计

**交付物：** 可以读取并显示所有配置的界面

### Week 2: 核心编辑功能

**目标：实现配置编辑和保存**

- [ ] Day 8-10: 配置编辑器
  - 实现JSON编辑器（Monaco Editor）
  - 实时语法验证
  - Diff预览功能

- [ ] Day 11-12: 写入逻辑
  - 实现 `write_config` 命令
  - 自动备份机制
  - 错误处理

- [ ] Day 13-14: Skills/MCP管理页面
  - Skills列表和详情
  - MCP服务器列表
  - 启用/禁用功能

**交付物：** 可以查看和编辑所有配置的完整应用

### Week 3: 完善和优化

**目标：打磨细节，准备发布**

- [ ] Day 15-16: 项目管理
  - 项目列表展示
  - 项目配置详情
  - CLAUDE.md编辑器

- [ ] Day 17-18: 优化和测试
  - 性能优化
  - 错误提示优化
  - 用户体验改进

- [ ] Day 19-21: 打包和文档
  - 应用图标和名称
  - 打包配置
  - README和使用文档

**交付物：** 可发布的MVP版本

---

## 十、关键依赖和工具

### 前端依赖

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "@tauri-apps/api": "^1.5.0",
    "zustand": "^4.4.0",
    "lucide-react": "^0.294.0",
    "@monaco-editor/react": "^4.6.0",
    "clsx": "^2.0.0",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
```

### UI组件库选择

**推荐：shadcn/ui**

**理由：**
- 基于Radix UI（无障碍性好）
- Tailwind CSS（快速定制）
- 复制粘贴到项目（完全控制）
- TypeScript友好
- 不增加打包体积

**组件使用：**
```typescript
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { Switch } from '@/components/ui/switch';
import { Dialog } from '@/components/ui/dialog';
import { Toast } from '@/components/ui/toast';
```

### 代码编辑器：Monaco Editor

**理由：**
- VS Code同款编辑器
- 优秀的TypeScript支持
- 内置JSON验证
- 轻量级集成

```typescript
import Editor from '@monaco-editor/react';

<Editor
  height="400px"
  defaultLanguage="json"
  value={jsonContent}
  onChange={(value) => setContent(value)}
  options={{
    minimap: { enabled: false },
    fontSize: 14,
    lineNumbers: 'on',
    automaticLayout: true,
  }}
/>
```

---

## 十一、安全性和错误处理

### 安全考虑

1. **配置文件备份**
   - 每次修改前自动创建 `.backup` 文件
   - 保留最近5个备份

2. **输入验证**
   - JSON Schema验证
   - 路径安全检查（防止目录遍历）

3. **权限管理**
   - 只允许操作ClaudeCode相关目录
   - 显示操作确认对话框

4. **敏感信息**
   - 检测API密钥等敏感信息
   - 提供脱敏显示选项

### 错误处理策略

```typescript
// 统一错误处理
try {
  await invoke('write_config', { path, content });
  toast.success('保存成功');
} catch (error) {
  // 根据错误类型显示不同提示
  if (error.message.includes('权限')) {
    toast.error('权限不足，请检查文件权限');
  } else if (error.message.includes('JSON')) {
    toast.error('JSON格式错误');
  } else {
    toast.error(`保存失败: ${error.message}`);
  }
}
```

---

## 十二、后续扩展方向（MVP之后）

### Phase 2: 增强功能（1-2个月）

- [ ] 配置模板系统
  - 保存常用配置为模板
  - 快速应用到新项目

- [ ] 配置对比工具
  - 对比不同项目配置
  - 可视化差异

- [ ] 批量操作
  - 批量启用/禁用Skills
  - 批量更新MCP配置

- [ ] 导入导出
  - 导出所有配置为JSON
  - 从备份恢复配置

### Phase 3: 高级特性（3-6个月）

- [ ] VS Code扩展版本
- [ ] 配置健康检查
- [ ] 配置文件搜索
- [ ] 快捷命令面板

---

## 十三、成功指标

### MVP成功标准

- ✅ 可以读取所有ClaudeCode配置
- ✅ 可以编辑并保存配置文件
- ✅ 可以启用/禁用Skills和MCP
- ✅ 启动时间<3秒
- ✅ 应用体积<20MB
- ✅ 无崩溃 bug

### 用户体验指标

- 首次使用5分钟内上手
- 常用操作<3次点击
- 清晰的错误提示
- 操作可撤销（备份恢复）

---

## 十四、总结：简化设计的核心优势

### 与原设计对比

| 维度 | 原设计 | 简化设计 | 改进 |
|------|--------|----------|------|
| 开发时间 | 3-6个月 | 2-3周 | **80%减少** |
| 应用体积 | ~150MB | ~15MB | **90%减少** |
| 启动时间 | ~5秒 | <1秒 | **5倍提升** |
| 功能数量 | 30+ | 10 | 聚焦核心 |
| 维护成本 | 高 | 低 | 更可持续 |

### 核心设计哲学

**"做一件事情，并把它做好"**

- ✅ **专注**：只解决配置可视化问题
- ✅ **简单**：被动读取，手动刷新
- ✅ **轻量**：最小依赖，快速启动
- ✅ **实用**：2-3周可用的MVP
- ✅ **可扩展**：为未来功能预留接口

---

## 附录：快速开始命令

```bash
# 创建Tauri项目
npm create tauri-app@latest claude-config-manager
# 选择: React + TypeScript

# 安装依赖
cd claude-config-manager
npm install

# 安装UI库
npx shadcn-ui@latest init
npx shadcn-ui@latest add button card switch dialog

# 安装其他依赖
npm install zustand lucide-react @monaco-editor/react date-fns

# 开发模式
npm run tauri dev

# 构建应用
npm run tauri build
```

---

**版本：** v2.0 简化版
**更新日期：** 2025-02-05
**预计MVP完成时间：** 2-3周
